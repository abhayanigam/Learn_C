#include <stdio.h>
int main()
{
    int n,i;
    printf("Enter the no. to check odd and even :");
    scanf("%d",&n);
    if (n%2==0)
    {
        printf("the no is the even :%d",n);
    }
    else
    {
        printf("the no is odd :%d",n);
    }
    
}