#include <stdio.h> // <header_file.h> used to include system header files
#include "header.h" // "header_file.h" used to include header files for your own program
/** above code will replace with content on header.h
    #include will copying .h file to the current line
    header files can only include once **/

int main() {
  puts(test());
  
  return 0;
}